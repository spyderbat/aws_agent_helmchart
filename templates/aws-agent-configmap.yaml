{{- range $.Values.awsAgentsConfigs }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $.Release.Name }}-{{.aws_account_id}}
  namespace: {{ $.Release.Namespace }}
data:
  aws-agent.yaml: |
    {{- . | toYaml | nindent 4 }}
    spyderbat_api_url: {{ $.Values.spyderbatApiUrl }}
    cluster_name: {{ $.Values.clusterName }}
{{- end }}

